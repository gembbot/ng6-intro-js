import { NgIntroJsDirective } from './ng-intro-js.directive';

describe('NgIntroJsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIntroJsDirective();
    expect(directive).toBeTruthy();
  });
});
